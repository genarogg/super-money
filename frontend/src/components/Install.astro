<section id="install" class="bg-gray-50 border-y border-gray-100 py-16">
  <div class="max-w-3xl mx-auto px-6">
    <h2 class="font-mono text-xs uppercase tracking-widest text-gray-400 mb-6">Instalación</h2>
    <div class="code-block"><span class="cmt"># instalar</span>
<span class="fn">pnpm</span> add <span class="str">super-money</span>

<span class="cmt"># en tu main.ts / main.jsx / layout.astro</span>
<span class="tag">import</span> initMoneyInputs <span class="tag">from</span> <span class="str">'super-money'</span>
initMoneyInputs()</div>

    <p class="mt-4 font-mono text-sm text-gray-400">
      A partir de ahí cualquier <code class="text-violet-500">&lt;input type="money"&gt;</code> del DOM funciona automáticamente, incluyendo los que se agreguen dinámicamente.
    </p>
  </div>
</section>
